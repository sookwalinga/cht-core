{
  "title": [
    {
      "locale": "en",
      "content": "Community Health Education"
    },
    {
      "locale": "sw",
      "content": "Elimu Ya Afya Ya Jamii"
    }
  ],
  "icon": "health-education",
  "context": {
    "person": true,
    "place": false,
    "expression": "contact && contact.type === 'person' && contact.parent && contact.parent.parent && contact.parent.parent.parent && !contact.date_of_death && !contact.muted && user.parent.type === 'health_center' && summary.show_new_modules"
  }
}
