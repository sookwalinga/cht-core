{
    "icon": "tb-treatment-outcome",
    "title": {
        "en": "TB Treatment Outcome", 
        "sw": "Matokeo ya Matibabu"
    },
    "context": {
    "person": true,
    "place": false,
     "expression": "contact && contact.type === 'person' && contact.parent && contact.parent.parent && contact.parent.parent.parent && !contact.date_of_death && !contact.muted && user.parent.type === 'health_center' && summary.should_show_tb_treatment_outcome===true && summary.show_new_modules"
  }
}
