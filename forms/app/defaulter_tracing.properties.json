{
  "title": [
    {
      "locale": "en",
      "content": "Defaulter Tracing"
    },
    {
      "locale": "sw",
      "content": "Defaulter wa TB"
    }
  ],
  "icon": "defaulter-tracing",
  "context": {
    "person": true,
    "place": false,
    "expression": "contact && contact.type === 'person' && contact.parent && contact.parent.parent && contact.parent.parent.parent && !contact.date_of_death && !contact.muted && !summary.is_contact_deceased && user.parent.type === 'health_center' && summary.should_show_defaulter_tracing===true && && summary.show_new_modules"
  }
}
