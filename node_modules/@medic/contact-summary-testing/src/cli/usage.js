module.exports = (status=0) => {
  console.log(`
medic-contact-summary-test
==========================

USAGE

	medic-contact-summary-test <contact-summary.js path> <tests parent directory>
`);
  process.exit(status);
};
