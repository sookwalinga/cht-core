{
  "name": "capacity-building-config",
  "version": "0.1.0",
  "description": "",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "eslint": "eslint *.json contact-summary*.js test/*.js",
    "test": "npm run eslint && npm run test-unit",
    "test-contact-summary": "medic-contact-summary-test",
    "test-unit": "mocha --require test/mocha-setup.js --reporter progress test/forms/*.js --timeout 2000000",
    "travis": "npm install && ./test/check-app-settings-is-committed && npm test"
  },
  "devDependencies": {
    "@medic/contact-summary-testing": "^1.1.3",
    "chai": "^4.2.0",
    "chai-exclude": "^1.0.8",
    "eslint": "^5.12.0",
    "eslint-plugin-json": "^1.3.2",
    "medic-conf": "^2.0.4",
    "medic-nootils": "^4.0.1",
    "mocha": "^5.1.1"
  },
  "dependencies": {
    "chai-date-string": "^0.1.0",
    "chai-datetime": "^1.5.0",
    "medic-conf-test-harness": "^0.1.20"
  }
}