{
  "name": "capacity-building-config",
  "version": "0.1.0",
  "description": "",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "jshint": "jshint *.json contact-summary*.js test/*.js",
    "test": "npm run jshint && npm run test-unit",
    "test-contact-summary": "medic-contact-summary-test",
    "test-unit": "mocha --require test/mocha-setup.js --reporter progress  --timeout 2000000",
    "travis": "npm install && NODE_OPTIONS=--max_old_space_size=4096 && ./test/check-app-settings-is-committed && NODE_OPTIONS=--max_old_space_size=4096 && npm test"
  },
  "devDependencies": {
    "@medic/contact-summary-testing": "^1.1.3",
    "chai": "^4.2.0",
    "chai-exclude": "^1.0.8",
    "medic-conf": "^3.2.0",
    "medic-nootils": "^4.0.1",
    "mocha": "^5.1.1"
  },
  "dependencies": {
    "chai-date-string": "^0.1.0",
    "chai-datetime": "^1.5.0",
    "medic-conf-test-harness": "^0.1.20"
  }
}
